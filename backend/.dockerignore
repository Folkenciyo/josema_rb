.venv
**/__pycache__
*.pyc
.pytest_cache
.ruff_cache
.git

# Downloaded at build time from free-exercise-db; never ship the local copy
# (~99 MB) into the build context.
app/static/exercise-images/

# Translation working files: only the merged artifact (app/data/*.json) is needed.
scripts/translation/batches/

# Client progress photos live in a Docker volume, never in the image or the repo.
app/media/
